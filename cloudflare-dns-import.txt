;; WalkThru Tours - DNS records for GitHub Pages
;; Cloudflare > walkthrutours.com > DNS > Records > Import > Select a file
;; Leave "Proxy imported DNS records" UNCHECKED.
;;
;; BEFORE IMPORTING, remove these two, and ONLY these two:
;;   1. Worker route on  walkthrutours.com  ->  lucky-union-daa5   (serves the apex)
;;      It is locked in the DNS list. Remove it from Workers & Pages >
;;      lucky-union-daa5 > Settings > Domains & Routes.
;;   2. CNAME  www  ->  ext-sq.squarespace.com   (the Coming Soon page)
;;   Optional: CNAME _domainconnect -> _domainconnect.domains.squarespace.com
;;      is a leftover from Squarespace and can go once you have left them.
;;
;; NEVER DELETE THESE. They are Google Workspace email for
;; willhainley@walkthrutours.com, and the whole outreach business runs on it:
;;   MX    walkthrutours.com        -> smtp.google.com
;;   TXT   walkthrutours.com        -> "v=spf1 include:_spf.google.com ~all"
;;   TXT   google._domainkey        -> "v=DKIM1; k=rsa; p=..."
;;   TXT   _dmarc                   -> "v=DMARC1; p=none; rua=..."
;;   TXT   walkthrutours.com        -> "google-site-verification=..."
;; Removing any of those sends your cold email straight to spam or bounces it.
;; An apex CNAME sitting next to MX records is fine here: Cloudflare flattens it.

walkthrutours.com.	1	IN	CNAME	willhainley.github.io.
www.walkthrutours.com.	1	IN	CNAME	willhainley.github.io.
